{
  "name": "CalendarProfile",
  "type": "object",
  "properties": {
    "business_id": {
      "type": "string",
      "description": "FK to Business \u2014 optional for personal calendars"
    },
    "user_email": {
      "type": "string",
      "description": "Email of the user who owns this personal calendar (used when business_id is not set)"
    },
    "name": {
      "type": "string",
      "description": "Calendar label (e.g. John's Schedule)"
    },
    "technician_name": {
      "type": "string"
    },
    "color": {
      "type": "string",
      "enum": [
        "blue",
        "green",
        "purple",
        "orange",
        "red",
        "pink"
      ],
      "description": "Color for this calendar"
    }
  },
  "required": [
    "name"
  ]
}